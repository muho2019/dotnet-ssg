<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts tagged '{{ tag }}' | {{ site.Title }}</title>
    <meta name="description" content="A collection of posts tagged with '{{ tag }}'.">

    <!-- SEO / Open Graph -->
    <link rel="canonical" href="{{ site.BaseUrl }}/tags/{{ tag }}/">
    <meta property="og:title" content="Posts tagged '{{ tag }}'">
    <meta property="og:description" content="A collection of posts tagged with '{{ tag }}'.">
    <meta property="og:url" content="{{ site.BaseUrl }}/tags/{{ tag }}/">
    <meta property="og:type" content="website">

    <link rel="stylesheet" href="/static/css/style.css">
</head>
<body>
    {% include '_includes/header.liquid' %}
    <main>
        <h2>Posts tagged: #{{ tag }}</h2>
        <ul>
            {% for post in posts %}
            <li>
                <h3><a href="{{ post.Url }}">{{ post.Title }}</a></h3>
                <p><time datetime="{{ post.Date | date.to_string '%Y-%m-%d' }}">{{ post.Date | date.to_string '%B %d, %Y' }}</time></p>
                {% if post.Description %}
                <p>{{ post.Description }}</p>
                {% endif %}
            </li>
            {% endfor %}
        </ul>
    </main>
    <footer>
        <p>&copy; {{ date.now | date.to_string "%Y" }} {{ site.Title }}</p>
    </footer>
</body>
</html>
