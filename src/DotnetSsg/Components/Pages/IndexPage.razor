<div class="max-w-notion mx-auto px-6 md:px-24 py-12">
    
    @* Notion 스타일 헤더 *@
    <header class="mb-12">
        <h1 class="text-notion-title font-bold mb-4">
            @Site.Title
        </h1>
        <p class="text-xl text-notion-gray">
            @Site.Description
        </p>
    </header>
    
    @* 포스트 목록 (Notion 데이터베이스 스타일) *@
    <div class="space-y-2">
        @foreach (var post in Posts)
        {
            <a href="@post.Url" 
               class="block group rounded-lg hover:bg-notion-gray-light dark:hover:bg-notion-gray-dark transition p-3 -mx-3">
                <article class="flex items-start gap-4">
                    
                    @* 아이콘 또는 이모지 *@
                    <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-notion-blue to-notion-green rounded flex items-center justify-center text-2xl">
                        📄
                    </div>
                    
                    @* 콘텐츠 *@
                    <div class="flex-1 min-w-0">
                        <h2 class="text-lg font-semibold text-notion-text dark:text-notion-text-dark group-hover:text-notion-blue transition truncate">
                            @post.Title
                        </h2>
                        
                        @if (!string.IsNullOrEmpty(post.Description))
                        {
                            <p class="text-sm text-notion-gray mt-1 line-clamp-2">
                                @post.Description
                            </p>
                        }
                        
                        <div class="flex flex-wrap items-center gap-4 mt-2 text-xs text-notion-gray">
                            <time datetime="@post.Date.ToString("yyyy-MM-dd")">
                                @post.Date.ToString("yyyy년 MM월 dd일")
                            </time>
                            
                            @if (post.Tags.Any())
                            {
                                <div class="flex flex-wrap gap-1">
                                    @foreach (var tag in post.Tags.Take(3))
                                    {
                                        <span class="px-1.5 py-0.5 bg-notion-gray-light dark:bg-notion-gray-dark rounded">
                                            @tag
                                        </span>
                                    }
                                    @if (post.Tags.Count > 3)
                                    {
                                        <span class="px-1.5 py-0.5">
                                            +@(post.Tags.Count - 3)
                                        </span>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                    
                </article>
            </a>
        }
    </div>
    
    @if (!Posts.Any())
    {
        <div class="text-center py-12 text-notion-gray">
            <p class="text-6xl mb-4">📭</p>
            <p>아직 포스트가 없습니다.</p>
        </div>
    }
    
</div>

@code {
    [Parameter, EditorRequired]
    public SiteConfig Site { get; set; } = null!;
    
    [Parameter, EditorRequired]
    public List<Post> Posts { get; set; } = null!;
}